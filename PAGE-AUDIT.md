# 页面交互检查（2026-09-08）

用户提供的 iPhone 记录显示：加载约 28 秒后仍为 interactive，jqueryReady=false，官网 initEntry、playSound、setupDoubleClick 函数已存在，但动词标题的 jQuery 点击绑定为 0，点击到达页面后也未展开。记录期间没有 DOMContentLoaded。这支持“初始化尚未完成、按钮尚未绑定”的判断；不能确定阻塞资源，也不能证明 Expo 是直接原因。

按用户授权，增加独立折叠点击处理，使用官网 entry.js 的 .unbox .box_title、.unbox .heading 及 is-active 状态。这覆盖采用此结构的动词形式、额外例句、词源和搭配。捕获阶段拦截匹配的标题点击，避免官网处理器重复切换。不调用或重跑整页初始化。临时页面检查入口及组件已移除。

验证：无官网处理器、已有处理器、重复注入、标题内嵌元素、展开收起、正文链接不被拦截。73 项项目测试和类型检查通过。这是针对折叠功能的兼容处理，不是加载阻塞根因修复，也不代表全站真机验收通过。

待真机复核：重新加载后，动词形式、额外例句、词源及搭配展开收起，展开后的发音和链接。既有发音与双击兼容处理仍保留。
